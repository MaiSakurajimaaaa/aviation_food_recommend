# 初稿.docx 引用修复指南

## 问题1：[8] 引用编号错误（必须修复）

**位置**：第2章 2.2.2节余弦相似度段第一句

**当前原文**：
```
余弦相似度（Cosine Similarity）是协同过滤中计算用户间相似度最常用的方法之一[6]
```

**问题**：[6]是钱聪（用户多重兴趣漂移特征权重），与余弦相似度无关。此处应引用改进用户评分相似度的协同过滤推荐算法。

**修复**：将 `[6]` 改为 `[8]`

---

## 问题2：[14]、[15] 未被正文引用（必须修复）

**位置**：第4章 4.2.1节总体架构段

**当前原文第1句**：
```
本系统采用前后端分离的B/S架构，整体分为三层...
```

**当前原文第2句**：
```
系统通过Nginx实现前端静态资源部署与API反向代理。
```

**问题**：[14]（杨硕, Python+Flask在线考试系统）和[15]（谢鸿博, 前后端分离架构下社交网络关系挖掘）在参考文献列表中但正文未出现对应编号。

**修复**：
- 第1句改为：`本系统采用前后端分离的B/S架构[14][15]，整体分为三层...`
- 第2句改为：`系统通过Nginx[14]实现前端静态资源部署与API反向代理。`

---

## 问题3：致谢导师名字错误（必须修复）

当前致谢中：**杨词慧、张胜、程世明**——这是模板（周彤）的导师。

你的开题报告指导教师是 **韩媛媛**。请重写致谢段落。

---

## 问题4：英文双引号改为中文引号（建议修复）

全文约39处英文 `"..."` 需改为中文 `""`。Word中操作：
1. `Ctrl+H` → 查找 `"` → 替换为 `"`（左中文引号U+201C）
2. `Ctrl+H` → 查找 `"` → 替换为 `"`（右中文引号U+201D）
3. 手动检查代码块区域，跳过程序代码中的引号

---

## 问题5：指导教师签名留空

封面页"指导教师: _____"请填写。

---

## 修复后校验

- [ ] 第2章"最常用的方法之一"后是 `[8]`，不是 `[6]`
- [ ] 第4章"前后端分离的B/S架构"后有 `[14][15]`
- [ ] 第4章"Nginx"后有 `[14]`
- [ ] 致谢导师名字已替换
- [ ] 英文双引号已改中文（代码块除外）
- [ ] 指导教师已填写
