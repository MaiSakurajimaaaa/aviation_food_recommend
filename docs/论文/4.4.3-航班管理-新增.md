### 4.4.3 航班管理

航班管理页面以表格形式列出所有航班，支持按航班号搜索。新增航班时需填写航班号、起降地、起降时间、餐食数量、餐食时段（JSON 数组，如 `["午餐","加餐"]`）以及预选截止时间。餐食数量由管理员手动指定，餐食时段根据餐食数量配置相应的用餐时段标签。系统根据起降时间自动计算飞行时长。每条航班记录提供编辑和删除操作。航班列表支持按状态筛选（启用/禁用），禁用的航班不在小程序端的候选航班列表中展示。
